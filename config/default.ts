export default {
  mcp: {
    // default allowed tools for MCP endpoints
    allowedTools: [
      'swagger:list-operations',
      'swagger:plan-run',
      'swagger:execute-operation',
      'swagger:generate-axios-tests'
    ]
  }
};
